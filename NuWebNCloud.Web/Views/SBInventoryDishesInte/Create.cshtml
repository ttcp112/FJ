@model NuWebNCloud.Shared.Integration.Models.Sandbox.Inventory.Product.InteProductModels
@using NuWebNCloud.Shared.Utilities;
@{
    ViewBag.Title = Html.Raw(_AttributeForLanguage.CurrentUser.GetLanguageTextFromKey("Dishes - Add New"));
}

<div class="se-pre-con"></div>
<div role="main" class="right_col">
    <div class="">
        <div class="page-title">
            <div class="title_left" style="padding: 10px 0px;">
                @Html.Partial("~/Views/Shared/_breadcrumb.cshtml",
                new Dictionary<string, string> { { "A", "Sandbox" }, { "B", "Inventory" }, { "C", "Dishes - Add New" } })
            </div>
            <div class="clearfix"></div>
            <div class="row">
                <div class="col-md-12 col-sm-12 col-xs-12 x_panel">
                    <div class="x_title">
                        @Html.Partial("_TabMenu", "create")
                        <div class="clearfix"></div>
                    </div>

                    <div class="x_content">
                        <div class="row text-right">
                            <ul class="header-buttons pull-right list-inline">
                                <li><a href="javascript:void(0);" class="btn btn-success" id="btn-actives" onclick="SubmitForm('.create-form')"><i class="fa fa-save"></i>&nbsp;
                                    @Html.Raw(_AttributeForLanguage.CurrentUser.GetLanguageTextFromKey("Save"))</a></li>
                                <li><a href="@Url.Action("Index", "SBInventoryDishesInte")" class="btn btn-danger" id="btn-delete"><i class="fa fa-times"></i>&nbsp;
                                    @Html.Raw(_AttributeForLanguage.CurrentUser.GetLanguageTextFromKey("Cancel"))</a></li>
                            </ul>
                        </div>
                        <div class="box-body" style="display: block;">
                            @using (@Html.BeginForm("Create", "SBInventoryDishesInte", FormMethod.Post, new { @class = "form-horizontal create-form", @enctype = "multipart/form-data" }))
                            {
                                @Html.Partial("_Form", Model)
                            }
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts{
    <script type="text/javascript">

        function disableButton(btn, status) {
            if (status) {
                $(btn).addClass('disabled');
            } else {
                $(btn).removeClass('disabled');
            }
        }

        function SubmitForm(form) {
            var isValid = $(form).valid();
            if (isValid) {
                disableButton('#btn-actives', true);
                $(form).submit();
            }

        }

        $(document).ready(function () {
        });
        //$('.create-form').submit(function () {
        //    ////disableButton('#btn-actives', true);
        //    ////$(this).submit();
        //    //return;

        //    $.ajax({
        //        url: $(this).attr('action'),
        //        type: "POST",
        //        //data: $(this).serialize(),
        //        data: new FormData(this),
        //        processData: false,
        //        contentType: false,

        //        beforeSend: function () {
        //            $('.se-pre-con').show();
        //        },
        //        complete: function () {
        //            $('.se-pre-con').hide();
        //        },
        //        success: function () {
        //            //var url = BaseUrl + ControllerName;
        //            //window.location.href = url;
        //            CloseDetail();
        //            Search();
        //        },
        //        error: function (jqXHR, textStatus, errorThrown) {
        //            $(".detail-view").html(jqXHR.responseText);
        //        }
        //    });
        //    return false;
        //});
    </script>
}
